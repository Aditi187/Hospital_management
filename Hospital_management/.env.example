# Example .env — copy to .env and add your real API keys locally.
# Do NOT commit your real .env to version control.

# Google Gemini API Key (RECOMMENDED - Free tier available)
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=AIzaSyA5JxuPFW05TenkVWvgY_XDvN6I_VywuS8

# OpenAI API Key (Alternative - Requires payment)


# After creating .env, restart the app so dotenv.load() picks it up.

# Optional: if you run the local proxy, set its URL here (do NOT commit real keys)
# Example to run proxy on localhost:3000
# OPENAI_PROXY_URL=http://localhost:3000
